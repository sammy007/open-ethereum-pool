import { formatBalance } from '../../../helpers/format-balance';
import { module, test } from 'qunit';

module('Unit | Helper | format balance');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatBalance(42);
  assert.ok(result);
});
