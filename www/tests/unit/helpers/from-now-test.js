import { fromNow } from '../../../helpers/from-now';
import { module, test } from 'qunit';

module('Unit | Helper | from now');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fromNow(42);
  assert.ok(result);
});
