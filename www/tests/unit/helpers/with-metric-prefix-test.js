import { withMetricPrefix } from 'pool/helpers/with-metric-prefix';
import { module, test } from 'qunit';

module('Unit | Helper | with metric prefix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = withMetricPrefix([42]);
  assert.ok(result);
});
