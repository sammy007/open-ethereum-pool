import { formatTx } from '../../../helpers/format-tx';
import { module, test } from 'qunit';

module('Unit | Helper | format tx');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatTx(42);
  assert.ok(result);
});
