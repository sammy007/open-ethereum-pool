import { formatDateLocale } from '../../../helpers/format-date-locale';
import { module, test } from 'qunit';

module('Unit | Helper | format date locale');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateLocale(42);
  assert.ok(result);
});
